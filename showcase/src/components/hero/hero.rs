use yew::{classes, function_component, html, Callback, Html, MouseEvent, Properties};

#[function_component(Hero)]
pub fn hero() -> Html {
    html! {
        <section>
            <h1>{"Text"}</h1>
        </section>
    }
}